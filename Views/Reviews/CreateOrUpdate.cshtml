@model TripBookingBE.Models.CustomerReviewTrip

@{
    ViewData["Title"] = Model.Id==0?"Create":"Edit";
}

<h1>@(Model.Id==0?"Create":"Edit")</h1>

@await Html.PartialAsync("_BookingForm.cshtml", Model, new ViewDataDictionary(ViewData){
    {"AllowedSave",true}
})