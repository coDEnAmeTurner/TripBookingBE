@model TripBookingBE.Models.Ticket

@if (ViewData["statusCode"] != null && ViewData["errorMessage"] != null)
{
    <div class="alert alert-danger" role="alert">
        <p><b>Status Code: @(ViewData["statusCode"].ToString())</b></p>
        <p><b>Message: @(ViewData["errorMessage"].ToString())</b></p>
    </div>
}


<h4>Ticket</h4>
<hr />
<form asp-action="CreateOrUpdate" method="post" class="container">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="CustomerBookTripId" />
    <input type="hidden" asp-for="DateCreated" />
    <input type="hidden" asp-for="DateModified" />
    @* <input type="hidden" asp-for="RowVersion" /> *@
    <div class="form-group col col-12">
        <label for="filter-customerId" class="form-label" style="font-weight: bold;">Customer: </label>
        <select disabled=@(ViewData["AllowedSave"].ToString().Equals("False"))
            class="form-control col col-12 form-select" asp-for="CustomerId" id="filter-customerId"
            asp-items="ViewBag.CustomerOptions">
            <option value=@(null)>Default</option>
        </select>
    </div>
    <div class="form-group col col-12">
        <label for="filter-tripId" class="form-label" style="font-weight: bold;">Trip: </label>
        <select disabled=@(ViewData["AllowedSave"].ToString().Equals("False"))
            class="form-control col col-12 form-select" asp-for="TripId" id="filter-tripId"
            asp-items="ViewBag.TripOptions">
            <option value=@(null)>Default</option>
        </select>
    </div>
    <div class="form-group col col-12">
        <label asp-for="Price" class="control-label col col-12" style="font-weight: bold;"></label>
        <input readonly=@(ViewData["AllowedSave"].ToString().Equals("False")) asp-for="Price"
            class="form-control col col-12" />
        <span asp-validation-for="Price" class="text-danger col col-12"></span>
    </div>
    <div class="form-group col col-12">
        <label asp-for="SellerCode" class="control-label col col-12" style="font-weight: bold;"></label>
        <select disabled=@(ViewData["AllowedSave"].ToString().Equals("False"))
            class="form-control col col-12 form-select" asp-for="SellerCode" id="filter-setllerCode"
            asp-items="ViewBag.SellerCodeOptions">
            <option value=@(null)>Default</option>
        </select>
        <span asp-validation-for="SellerCode" class="text-danger col col-12"></span>
    </div>
    <div class="form-group col col-12">
        <label for="filter-generalParamId" class="form-label" style="font-weight: bold;">General Param: </label>
        <select disabled=@(ViewData["AllowedSave"].ToString().Equals("False"))
            class="form-control col col-12 form-select" asp-for="GeneralParamId" id="filter-generalParamId"
            asp-items="ViewBag.GeneralParamOptions">
            <option value=@(null)>Default</option>
        </select>

    </div>
    <div class="form-group col col-12 mt-2 justify-content-center">
        <input disabled=@(ViewData["AllowedSave"].ToString().Equals("False")) type="submit" value="Save"
            class="btn btn-primary" />
        <a class="btn btn-dark" asp-action="Index">Back to List</a>
    </div>
</form>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<script type="text/javascript">
    $(function () {
        $("#datepicker").datepicker({
            dateFormat: "dd/mm/yy"
        });
    });
</script>